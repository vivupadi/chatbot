apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-pvc
  namespace: rag-dev          # matches your existing namespace
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi            # reduced from 5Gi — you only have 7.8GB free
                              # phi model = 1.6GB, leaves 1.4GB buffer
