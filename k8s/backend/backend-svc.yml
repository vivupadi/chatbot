apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: rag-dev
  labels:
    app: backend
spec:
  type: ClusterIP  # Internal only
  selector:
    app: rag-backend
    tier: application
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http